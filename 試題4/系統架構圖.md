# 試題4：系統架構圖

本文件繪製試題1、試題2、試題3的整體系統架構，說明爬蟲、資料庫、API、Log 收集器與異常通知之間的關係。

## 一、整體系統架構圖

```mermaid
flowchart LR
    User[操作人員 / 使用者]
    RIS[內政部戶政司 RIS 3053 網站]

    subgraph T1[試題1：爬蟲程式]
        CrawlerMain[main.py\n爬蟲主流程]
        RisClient[ris_client.py\nHTTP / CSRF / CAPTCHA / 查詢]
        Storage[storage.py\nraw / CSV / SQLite / Log]
        CrawlerNotify[notification.py\n爬蟲異常通知]
    end

    subgraph T1Output[試題1輸出]
        RawJSON[data/raw/*.json\n原始回應先落檔]
        CSV[data/csv/*.csv\n結構化CSV]
        SQLite[(data/ris_doorplate.sqlite3\nSQLite資料庫)]
        CrawlerLogs[logs/crawler-info.log\nlogs/crawler-error.log\nlogs/crawler-job.jsonl]
    end

    subgraph T2[試題2：後端API]
        ApiMain[main.py\nHTTP API Server]
        Repo[repository.py\nSQLite查詢層]
        ApiNotify[notification.py\n空查詢異常通知]
        ApiQueryLog[logs/api-query.jsonl\nAPI查詢紀錄]
    end

    subgraph T3[試題3：Log收集器與異常通知平台]
        LogCollector[main.py\nLog Collector Dashboard]
        Notifications[notifications/notifications.jsonl\n異常通知紀錄]
        Dashboard[瀏覽器平台\nhttp://127.0.0.1:9000]
    end

    User -->|輸入 CAPTCHA / 啟動爬蟲| CrawlerMain
    CrawlerMain --> RisClient
    RisClient -->|GET/POST 查詢| RIS
    RIS -->|HTML / CAPTCHA / JSON| RisClient
    RisClient --> CrawlerMain
    CrawlerMain --> Storage
    Storage --> RawJSON
    Storage --> CSV
    Storage --> SQLite
    Storage --> CrawlerLogs
    CrawlerMain -->|例外時寫通知| CrawlerNotify
    CrawlerNotify --> Notifications

    User -->|HTTP查詢 /records /jobs /health| ApiMain
    ApiMain --> Repo
    Repo --> SQLite
    ApiMain --> ApiQueryLog
    ApiMain -->|查詢結果為空| ApiNotify
    ApiNotify --> Notifications

    LogCollector --> CrawlerLogs
    LogCollector --> ApiQueryLog
    LogCollector --> Notifications
    User --> Dashboard
    Dashboard --> LogCollector
```

## 二、資料流架構圖

```mermaid
sequenceDiagram
    participant User as 操作人員
    participant Crawler as 試題1爬蟲
    participant RIS as RIS 3053網站
    participant Raw as Raw JSON
    participant CSV as CSV檔
    participant DB as SQLite資料庫
    participant API as 試題2 API
    participant Log as 試題3 Log平台
    participant Noti as 異常通知JSONL

    User->>Crawler: python main.py --once
    Crawler->>RIS: 開啟查詢頁，取得 CSRF / captchaKey
    RIS-->>Crawler: HTML + CAPTCHA key
    Crawler-->>User: 顯示 CAPTCHA 圖檔路徑
    User->>Crawler: 人工輸入 CAPTCHA
    Crawler->>RIS: 以編釘日期、編釘類別查詢
    RIS-->>Crawler: JSON查詢結果
    Crawler->>Raw: 先保存原始JSON
    Crawler->>CSV: 輸出清理後CSV
    Crawler->>DB: Upsert結構化資料
    Crawler->>Log: 寫入crawler-info/error/job log

    User->>API: GET /records 或 /jobs
    API->>DB: 查詢SQLite
    DB-->>API: 回傳資料
    API-->>User: JSON response
    API->>Log: 寫入api-query.jsonl

    alt 爬蟲異常
        Crawler->>Noti: 寫入crawler異常通知
    end

    alt API查詢資料為空
        API->>Noti: 寫入empty result通知
    end

    User->>Log: 開啟 http://127.0.0.1:9000
    Log->>Log: 讀取crawler log / api log / notifications
    Log-->>User: 顯示即時Dashboard
```

## 三、元件職責說明

| 試題 | 元件 | 職責 |
| --- | --- | --- |
| 試題1 | `main.py` | 爬蟲入口、排程、分頁查詢、人工 CAPTCHA 流程 |
| 試題1 | `ris_client.py` | 維持 Cookie、取得 CSRF、下載 CAPTCHA、送出查詢 POST |
| 試題1 | `storage.py` | raw JSON 落檔、CSV輸出、SQLite建表與upsert、job log |
| 試題1 | `notification.py` | 爬蟲異常時寫入試題3通知檔，可選 webhook |
| 試題2 | `main.py` | 提供 `/health`、`/records`、`/records/{id}`、`/jobs` API |
| 試題2 | `repository.py` | 封裝 SQLite 查詢與條件組合 |
| 試題2 | `notification.py` | 查詢結果為空時寫入異常通知 |
| 試題3 | `main.py` | Log Dashboard 與 JSON API，讀取試題1/2 log 與通知 |
| 試題4 | `系統架構圖.md` | 說明整體系統架構、資料流與部署方式 |

## 四、部署 / 執行關係

```mermaid
flowchart TB
    Start[開始]
    RunCrawler[執行試題1\npython main.py --once]
    CheckDB{是否產生SQLite?}
    RunAPI[執行試題2\npython main.py]
    RunLog[執行試題3\npython main.py]
    BrowserAPI[測試API\nhttp://127.0.0.1:8000/health]
    BrowserLog[檢視Log平台\nhttp://127.0.0.1:9000]

    Start --> RunCrawler
    RunCrawler --> CheckDB
    CheckDB -->|是| RunAPI
    CheckDB -->|否，檢查CAPTCHA或查詢條件| RunCrawler
    RunAPI --> BrowserAPI
    BrowserAPI --> RunLog
    RunLog --> BrowserLog
```

## 五、異常通知設計

```mermaid
flowchart LR
    CrawlerError[試題1爬蟲異常\nHTTP錯誤 / JSON解析錯誤 / 分頁失敗]
    ApiEmpty[試題2查詢為空\n/records total=0\n/records/id 404]
    NotifyHelper[notification.py]
    NotifyFile[試題3/notifications/notifications.jsonl]
    Webhook[可選：NOTIFY_WEBHOOK_URL]
    Dashboard[試題3 Dashboard]

    CrawlerError --> NotifyHelper
    ApiEmpty --> NotifyHelper
    NotifyHelper --> NotifyFile
    NotifyHelper -.若有設定.-> Webhook
    NotifyFile --> Dashboard
```

通知採 JSONL 格式，每行一筆事件，包含：

```json
{
  "created_at": "2026-06-09T00:00:00+00:00",
  "service": "試題1爬蟲",
  "severity": "error",
  "event_type": "crawler_page_failed",
  "message": "爬蟲分頁任務失敗",
  "details": {}
}
```

## 六、設計重點

1. **先落檔再入庫**：試題1先保存 raw JSON，再輸出 CSV，最後寫 SQLite，方便追查與重跑解析。
2. **資料服務分層**：試題2 API 不直接爬網站，只讀取試題1產生的 SQLite。
3. **監控獨立化**：試題3獨立讀 log 與 notification，不影響爬蟲/API 主流程。
4. **異常可追蹤**：爬蟲失敗、API空查詢都會寫入 JSONL 通知紀錄。
5. **標準庫實作**：所有試題皆使用 Python 標準庫，Windows 可直接執行。
